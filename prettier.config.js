/** @type {import('prettier').Config} */
const config = {
    tabWidth: 2,
    singleQuote: true,
    trailingComma: 'es5',
    arrowParens: 'always',
    printWidth: 100,
    endOfLine: 'lf',
    htmlWhitespaceSensitivity: 'ignore',
};

module.exports = config;
